import {makeStyles} from '@material-ui/core'
export const useStyles = makeStyles({
  root: {
    height: '200px',
    width: '150px',
    borderRadius: '10px',
    textAlign: 'center',
    backgroundColor: '#F6F6F6',
  },
  title: {
    whiteSpace: 'nowrap',
  },
})
