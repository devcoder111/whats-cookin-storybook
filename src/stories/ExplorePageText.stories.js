import React from "react";
import ExplorePageText from "../components/ExplorePageText/index";

export default {
  title: "Explore Page Text",
  component: ExplorePageText,
};

const Template = (args) => <ExplorePageText {...args} />;

export const text = Template.bind({});
